name: tarim_proje
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_colorpicker: ^1.0.3
  firebase_auth_web: ^5.8.13
  table_calendar: ^3.2.0
  intl: ^0.20.2
  flutter_drawing_board: ^0.9.8
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.5
  firebase_auth: ^5.5.1
  cached_network_image: ^3.4.1
  js: ^0.7.2
  url_launcher: ^6.3.1
  youtube_player_flutter: ^8.1.0
  camera: ^0.10.5+9
  tflite_flutter: ^0.10.0  # Burada eklenen tflite_flutter paketi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
